# 快办 / Act Now

Act Now は、日々のタスクをすぐ確認できる Windows 向けの軽量なデスクトップ付箋 Todo アプリです。

## 機能

- Todo：追加、完了、削除に対応し、完了したタスクは自動でアーカイブされます。
- 作成時間：各 Todo の左下に作成時間を表示します。
- グループ：すべての Todo とカスタムグループを利用できます。グループを削除すると、その中の Todo はデフォルトグループへ移動します。
- アーカイブ：完了済み Todo の復元、個別削除、全消去に対応します。
- 画面端に隠す：左、右、上の画面端に隠せます。下端では自動非表示になりません。
- 上端自動収納：ウィンドウを画面上端へドラッグすると隠れ、マウスを近づけると表示されます。
- ウィンドウ操作：ドラッグ移動、ドラッグリサイズ、タスクバーへの最小化、閉じるボタンの動作設定に対応します。
- 自動起動：Windows 起動時に自動起動できます。
- 多言語：中文、English、日本語に対応し、メイン画面とトレイメニューも切り替わります。
- Material You 風：明るい配色、柔らかいコンテナ色、見やすい入力欄、タイトルバーのホバー表示を備えています。
- タスクバーとトレイ：起動中は Windows のタスクバーとシステムトレイに表示されます。

## インストール

推奨インストーラー：

```text
release\快办安装程序.exe
```

インストール時にインストール先フォルダーを選択でき、デスクトップとスタートメニューのショートカットも作成できます。インストール後は Windows のアンインストール一覧に表示されます。

直接実行する場合：

```text
release\快办.exe
```

## ソースから実行

Windows と Python 3 が必要です。

```powershell
python sticky_todo.py
```

`启动便签.bat` を実行することもできます。

## データ保存場所

ユーザーデータは以下に保存されます。

```text
%APPDATA%\ActNow\data.json
```

## ビルド

このプロジェクトは PyInstaller でパッケージ化します。

```powershell
scripts\build_release.ps1
```

生成されるファイル：

```text
release\快办.exe
release\快办安装程序.exe
```

## 作者

- 作者：執簡
- WeChat：vdc089
- GitHub：https://github.com/vodecs001/Act-Now

## 注意

サードパーティ製フォントは同梱していません。アプリアイコンと QR コードはプロジェクト用素材です。公開配布する前に使用権限を確認してください。
